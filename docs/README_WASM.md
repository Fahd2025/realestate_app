# Add WASM files to web directory
# These files are needed for Drift WASM support
# They will be automatically downloaded by Drift when you run the app

# For now, we'll use a simpler web database approach
# The WASM files will be generated when you first run the app
